# H2 Console
#spring.h2.console.enabled=true
#spring.h2.console.path=/h2-console

# Session Timeout
spring.session.timeout=86400

# Datasource
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.datasource.url=jdbc:mariadb://${DB_HOST}:${DB_PORT}/testdb?characterEncoding=UTF-8&serverTimezone=UTC
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# Pageable
spring.data.web.pageable.max-page-size=2000
spring.data.web.pageable.default-page-size=10
spring.data.web.pageable.one-indexed-parameters=true

# Redis
spring.data.redis.host=127.0.0.1
spring.data.redis.port=6379

# JPA
spring.jpa.defer-datasource-initialization=true
spring.jpa.hibernate.ddl-auto=create
spring.session.jdbc.initialize-schema: always
spring.jpa.properties.hibernate.format_sql=true

# Server
server.servlet.session.cookie.max-age=172800

# multipartFile \uC6A9\uB7C9 \uC81C\uD55C
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.enabled=true

# Date And Time format
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss

# Logging
logging.level.sql=debug
logging.level.type=trace

# thymeleaf
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.encoding=UTF-8

# \uBA54\uC77C \uC124\uC815
# \uAE43\uD5C8\uBE0C \uC5C5\uB85C\uB4DC \uC804\uC5D0 \uC815\uBCF4\uB97C \uACC4\uC815 \uC815\uBCF4\uC640 \uBE44\uBC00\uBC88\uD638\uB97C \uC81C\uAC70\uD560\uAC83
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=goormhotel
spring.mail.password=galmgtdbxdxavkve
spring.mail.properties.mail.smtp.auth = true
spring.mail.properties.mail.smtp.timeout = 5000;

# response\uC758 error \uC815\uBCF4 \uC804\uB2EC
server.error.include-message=always
server.error.include-binding-errors=always

# If you want to file upload another location, change below
file.upload = C:/upload/
resource.handler = /mail/upload/img/**
resource.location = file:///C:/upload/